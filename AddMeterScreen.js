'use strict';

var React = require('react-native');
var {
  StyleSheet,
  View,
  Text
} = React;

var AddMeterScreen = React.createClass({
  render: function() {
    return (
      <Text />
    );
  },
});

var styles = StyleSheet.create({
  meterContainer: {
    marginTop: 64
  }
});

module.exports = AddMeterScreen;
